{{define "title"}}New comment{{end}}
{{template "head" .}}
<p class="header"><a href="/">Mochi</a> | <a href="/feeds">Feeds</a> | <a href="/feeds/{{.feed.id}}">{{.feed.name}}</a> | New comment

<form action="/feeds/{{.feed.id}}/{{.post}}/create">
	<textarea name="body" rows="20" cols="100" placeholder="Enter comment, markdown is allowed"></textarea><br/>
	<input type="hidden" name="parent" value="{{.parent}}"/>
    <input type="submit" value="Comment"/>
</form>

{{template "foot" .}}
